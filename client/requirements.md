## Packages
date-fns | Date formatting and calculation needed for streaks and logs
canvas-confetti | For milestone celebrations (confetti effects)
@types/canvas-confetti | Types for confetti
framer-motion | Smooth animations for page transitions and micro-interactions
recharts | Visualizing usage trends over time

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
